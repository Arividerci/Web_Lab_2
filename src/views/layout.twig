<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    {% block styles %}
        <link rel="stylesheet" href="/assets/css/style.css">
    {% endblock %}
</head>
<body>

<header class="site-header">
  <div class="header-container">
    <div class="header-left">
      <div class="logo">🛒 Электроника</div>
      <nav class="nav-menu">
        <a href="/">Главная</a>
        <a href="/submit">Корзина</a>

        {% if session.user is defined and session.user.role == 'admin' %}
          <a href="/orders">Заказы</a>
        {% endif %}
      </nav>
    </div>
    <div class="header-right">
        {% if session.user is defined %}
        <a href="/cabinet">
            <img src="/assets/images/User_Avatar_D.png" alt="Профиль" class="user-avatar">
        </a>
        {% else %}
            <a class="login-link" href="/login">Войти</a>
        {% endif %}

    </div>

  </div>
</header>


<div class="container">
    <div class="register-container">
        {% block content %}{% endblock %}
    </div>
</div>
</body>
</html>
